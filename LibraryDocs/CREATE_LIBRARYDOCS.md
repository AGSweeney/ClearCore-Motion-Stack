# CREATE_LIBRARYDOCS

This `LibraryDocs/` tree was produced with the global Cursor skill
`create-librarydocs` (v2.1.0).

Validate from the repository root:

```powershell
python "$env:USERPROFILE\.cursor\skills\create-librarydocs\scripts\validate_librarydocs.py" `
  --repo-root . `
  --strict
```

Do not copy the full skill into this repository; keep only `LibraryDocs/`.
